description: "Accepted"

content:
  application/json:
    schema:
      properties:
        status:
          type: "string"
          example: "Accepted"
        metadata:
          type: "object"
        data:
          type: "object"
      required:
        - "status"
