description: "OK"

content:
  application/json:
    schema:
      properties:
        status:
          type: "string"
          example: "OK"
        metadata:
          type: "object"
        data:
          type: "object"
      required:
        - "status"
        - "data"
