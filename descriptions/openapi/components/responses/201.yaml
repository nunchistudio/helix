description: "Created"

content:
  application/json:
    schema:
      properties:
        status:
          type: "string"
          example: "Created"
        metadata:
          type: "object"
        data:
          type: "object"
      required:
        - "status"
